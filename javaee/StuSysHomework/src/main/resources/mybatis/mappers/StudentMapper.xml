<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.stusyshomework.mapper.StudentMapper">

    <resultMap id="StudentResultMap" type="StudentMB">
        <id property="studentId" column="student_no"/>
        <result property="name" column="name"/>
        <result property="major" column="department"/>
        <result property="grade" column="class_name"/>
    </resultMap>

    <select id="findAll" resultMap="StudentResultMap">
        SELECT student_no, name, department, class_name
        FROM students
        WHERE status = '在读'
        ORDER BY student_no
    </select>

    <select id="findByStudentId" resultMap="StudentResultMap">
        SELECT student_no, name, department, class_name
        FROM students
        WHERE student_no = #{studentId}
    </select>

</mapper>
